apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sourcebans
  namespace: sourcebans
spec:
  secretName: sourcebans-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - bans.udl.tf
