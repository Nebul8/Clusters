apiVersion: v1
kind: Service
metadata:
  name: citadel-webserver
  namespace: citadel
spec:
  selector:
    app: citadel-webserver
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP